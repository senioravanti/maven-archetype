#set ( $d = "$")
<?xml version="1.0" encoding="UTF-8"?>
<Configuration
    xmlns="https://logging.apache.org/xml/ns"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
    https://logging.apache.org/xml/ns
    https://logging.apache.org/xml/ns/log4j-config-2.xsd"
>
    <Properties>
        <Property
            name="LOG_DIR_PROP"
            value="${d}{env:LOG_DIR:-./logs}"
        />
        <Property
            name="LOG_FILE_PROP"
            value="${d}{env:LOG_FILE:-app}"
        />
        <Property
            name="FILE_EXT_PROP"
            value="log.jsonl"
        />
        <Property
            name="LOG_LEVEL_APP_PROP"
            value="${d}{env:LOG_LEVEL_APP:-DEBUG}"
        />
    </Properties>
    <Appenders>
        <RollingFile
            name="File"
            fileName="${d}{LOG_DIR_PROP}/${d}{LOG_FILE_PROP}.${d}{FILE_EXT_PROP}"
            filePattern="${d}{LOG_DIR_PROP}/${d}${d}{date:yyyy-MM}/${d}{LOG_FILE_PROP}-%d{yyyy-MM-dd}-%i.${d}{FILE_EXT_PROP}.gz"

            bufferSize="2048"
            append="false"
            immediateFlush="true"
        >
            <PrettyStructuredLoggingFormatter/>
            <DefaultRolloverStrategy max="100"/>
            <Policies>
                <CronTriggeringPolicy schedule="0 0 0 1 * ?"/>
                <SizeBasedTriggeringPolicy size="10 MB"/>
            </Policies>
        </RollingFile>

    </Appenders>
    <Loggers>
        <Root level="INFO">
            <AppenderRef ref="File"/>
        </Root>
        <Logger
            name="${groupId}.${artifactId}"
            level="${d}{LOG_LEVEL_APP_PROP}"
        />
    </Loggers>
</Configuration>